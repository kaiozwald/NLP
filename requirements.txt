# requirements.txt
# Cross-Lingual Persona Consistency project
#
# INSTALL (Mac Apple Silicon, from project root):
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# -- API client ----------------------------------------------------------------
groq>=0.9.0                  # Groq API client (Mistral + Llama, free tier)

# -- Sentence embeddings (consistency metric) ----------------------------------
sentence-transformers>=2.7.0

# -- Sentiment analysis --------------------------------------------------------
transformers>=4.40.0         # for multilingual sentiment model (CPU only, lightweight)
torch>=2.2.0                 # needed by sentence-transformers + transformers

# -- Data handling -------------------------------------------------------------
pandas>=2.0.0
numpy>=1.26.0

# -- Visualisation -------------------------------------------------------------
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0

# -- Notebook ------------------------------------------------------------------
jupyter>=1.0.0
ipykernel>=6.29.0

# -- Utilities -----------------------------------------------------------------
tqdm>=4.66.0
python-dotenv>=1.0.0